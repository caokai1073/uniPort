# Runtime dependencies of the uniport package.
# Optional extras (example notebooks, docs) live in pyproject.toml:
#   pip install -e '.[examples]'   pip install -e '.[docs]'
numpy>=1.23
scipy>=1.9
pandas>=1.5
scikit-learn>=1.1
torch>=2.0
anndata>=0.8
scanpy>=1.9
tqdm>=4.60
