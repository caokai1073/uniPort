# Documentation build dependencies, consumed by /.readthedocs.yaml.
#
# The package itself (and therefore its runtime dependencies) is installed
# separately by that file, because conf.py imports uniport for the version and
# autosummary imports every documented module.
#
# torch is pulled from the CPU-only index: the default PyPI wheels bundle the
# CUDA runtime, which is several GB and does not fit in a docs build.
--extra-index-url https://download.pytorch.org/whl/cpu

sphinx>=7,<9
sphinx-rtd-theme>=2.0
sphinx-copybutton>=0.5
sphinx-markdown-tables>=0.0.15
myst-parser>=2.0
nbsphinx>=0.9
